# Main training/evaluation environment (Python 3.10).
# JEDi evaluation (scripts/video_jedi.py) requires a separate environment: see requirements-jedi.txt.
# mpi4py is NOT listed here: install it via conda together with its MPI runtime BEFORE this file
# (see README Installation): conda install mpich=3.3.2 mpi4py=3.1.4
torch==2.2.1
torchvision==0.17.1
numpy==1.26.4
scipy==1.12.0
blobfile==2.1.1
tqdm==4.66.2
wandb[media]==0.29.0  # [media] pulls moviepy, needed for logging sample videos
filelock==3.13.1
imageio==2.34.0
matplotlib==3.8.4
seaborn>=0.13
pandas==2.2.2
scikit-learn==1.5.2
six==1.16.0
opencv-python==4.9.0.80
Pillow==10.2.0
ffmpeg-python==0.2.0
diffusers==0.26.3
huggingface_hub==0.25.2  # diffusers 0.26 imports cached_download, removed in huggingface_hub 0.26
accelerate==0.27.2       # lets diffusers load the SDXL VAE without materialising it in host RAM first
transformers==4.28.0
einops==0.8.1
timm==1.0.15
lpips==0.1.4
# TensorFlow is only needed for FVD/KVD evaluation (scripts/video_fvd.py).
tensorflow==2.8.0
tensorflow-hub==0.12.0
